{
    "$schema": "https://turbo.build/schema.json",
    "ui": "tui",
    "tasks": {
        "clean": {
            "dependsOn": ["^clean"],
            "cache": false
        },
        "build": {
            "dependsOn": ["clean", "^build"],
            "outputs": ["dist/**"]
        }
    }
}
